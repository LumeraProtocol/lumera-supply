# Example environment file for the native (non-Docker) systemd unit
# Copy to: /etc/default/lumera-supply-native

# Address to listen on
LUMERA_HTTP_ADDR=:8080

# Cosmos LCD base URL
LUMERA_LCD_URL=https://lcd.lumera.io

# Default base denom
LUMERA_DEFAULT_DENOM=ulume

# Path to the policy JSON file on the host
LUMERA_POLICY_PATH=/etc/lumera/policy.json
